version: 2
jobs:
  build:
    working_directory: /yonatankoren
    docker:
      - image: docker:17.05.0-ce-git
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Build Docker Image 
          command: docker build .
